# Environment Variables Template
# Copy this file to .env for local development

# Redirect URL - if set, users will be redirected to this URL once per session
# Leave empty or comment out to disable redirect
REDIRECT_URL=

# Session secret for secure session management (change in production)
SESSION_SECRET=your-secret-key-change-in-production

# Iframe URL to be embedded in the home page
IFRAME_URL=https://example.com

# Port (optional, defaults to 3000)
PORT=3000
